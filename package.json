{
  "dependencies": {
    "axios": "^1.3.4",
    "discord.js": "^14.8.0",
    "typescript": "^5.0.2",
    "yaml": "^2.2.1"
  },
  "name": "discordjs-bot",
  "description": "",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cp -f config.yml out/. && npx tsc && node out/deploy-commands.js && node out/index.js",
    "dev": "cp -f config.yml out/. && npx tsc && node --inspect out/deploy-commands.js && node --inspect out/index.js"
  },
  "author": "",
  "license": "ISC"
}
